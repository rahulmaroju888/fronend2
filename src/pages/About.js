import React from "react";
import HeroSection from "../components/HeroSection";

function About() {
  const data = {
    name: "Rahul E-Commerce",
  };
  return <HeroSection myData={data} />;
}

export default About;
